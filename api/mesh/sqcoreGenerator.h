#ifndef SQCORE_GENERATOR_30_09_2013
#define SQCORE_GENERATOR_30_09_2013

/*
#include <boost/shared_ptr.hpp>

#include "blueprint\site.h"

//#ifdef MINAX

class sgCGroup;
namespace Generator
{

    struct SGCoreParams
    {
        Blueprint::Site::Ptr pSite;
        double dHeight, dBorder, dScale;
        bool bDoSubtraction;

        SGCoreParams( Blueprint::Site::Ptr _pSite,
                double _dHeight, double _dBorder,
                double _dScale, bool _bDoSubtraction )
                :   pSite( _pSite ),
                    dHeight( ( _dHeight > 3.0 ) ? _dHeight : 3.0 ),
                    dBorder( ( _dBorder > 1.0 ) ? _dBorder : 1.0 ),
                    dScale( _dScale ),
                    bDoSubtraction( _bDoSubtraction )
        {

        }
    };
    
boost::shared_ptr< sgCGroup > generateBlueprint_sgcore( const SGCoreParams& params );

}
*/

#endif //SQCORE_GENERATOR_30_09_2013